// Package glmocr implements local document OCR through the GLM-OCR SDK's
// page-aware HTTP service. It consumes structured layout regions rather than a
// generic chat-completions string.
package glmocr
